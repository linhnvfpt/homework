# Write a Python program that will accept 
# the base and height of a triangle and compute the area
base = float(input('Input the base: '))
height = float(input('Input the height: '))
area_triangle = 1/2 * base * height
print ('Area triangle: ',area_triangle)